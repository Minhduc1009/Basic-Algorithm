// use toStringArray and sort and equal

public static boolean charactersRearrangement(String s1, String s2) {
		char[] a = s1.toCharArray();
		char[] b = s2.toCharArray();
		
		Arrays.sort(a);
		Arrays.sort(b);
		
		return Arrays.equals(a, b);	
	}
  
  // toCharArray: biến chuỗi thành mảng
  // sort: sắp xếp chuỗi
  // equals: so sánh hai chuỗi
  
